import 'package:flutter/material.dart';

class FirstPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("CoffeeMix"),
        backgroundColor: new Color(0xFFD7413E),
      ),
      body: new Center(
        child: new Text("ZIBINK"),
      ),
    );
  }
}
